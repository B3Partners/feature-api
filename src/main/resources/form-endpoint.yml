openapi: 3.0.1
info:
  title: OpenAPI definition
  version: v0
servers:
- url: http://localhost:8080
  description: Generated server url
paths:
  /features/{application}/{appLayerId}/{x}/{y}/{scale}:
    get:
      tags:
      - feature-controller
      operationId: onPoint
      parameters:
      - name: application
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: appLayerId
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: x
        in: path
        required: true
        schema:
          type: number
          format: double
      - name: "y"
        in: path
        required: true
        schema:
          type: number
          format: double
      - name: scale
        in: path
        required: true
        schema:
          type: number
          format: double
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Feature'
  /features/2/{application}/{appLayerIds}/{x}/{y}/{scale}:
    get:
      tags:
      - feature-controller
      operationId: onPoint_1
      parameters:
      - name: application
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: appLayerIds
        in: path
        required: true
        schema:
          type: array
          items:
            type: integer
            format: int64
      - name: x
        in: path
        required: true
        schema:
          type: number
          format: double
      - name: "y"
        in: path
        required: true
        schema:
          type: number
          format: double
      - name: scale
        in: path
        required: true
        schema:
          type: number
          format: double
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Feature'
components:
  schemas:
    Attribute:
      type: object
      properties:
        type:
          type: string
        key:
          type: string
        value:
          type: object
    Feature:
      type: object
      properties:
        clazz:
          type: string
        attributes:
          type: array
          items:
            $ref: '#/components/schemas/Attribute'
        relations:
          type: array
          items:
            $ref: '#/components/schemas/Relation'
        children:
          type: array
          items:
            $ref: '#/components/schemas/Feature'
        fid:
          type: object
    Relation:
      type: object
      properties:
        filter:
          type: string
        foreignFeatureTypeId:
          type: integer
          format: int64
        foreignFeatureTypeName:
          type: string
